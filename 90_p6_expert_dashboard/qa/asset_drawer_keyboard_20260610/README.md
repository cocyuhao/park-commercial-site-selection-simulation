# 资料池键盘交互 QA（2026-06-10）

## 修改

- 资料池按钮会明确表示当前展开或关闭状态。
- 打开资料池后，键盘焦点自动进入关闭按钮。
- 按 `Esc` 可关闭资料池，并把焦点返回“资料池”按钮。
- 为资料池按钮补充稳定测试标记。

## 验证

- 桌面宽度：1440。
- 移动端宽度：390。
- 打开前：资料池关闭，按钮状态为未展开。
- 打开后：资料池可见，按钮状态为已展开，焦点位于关闭按钮。
- 按 `Esc` 后：资料池关闭，焦点返回资料池按钮。
- 页面控制台错误：0。

## 截图

- `asset_drawer_keyboard_open.png`
- `asset_drawer_keyboard_mobile.png`

## 修改边界

- 未修改核心模型、知识库、证据链、仿真任务、报告生成逻辑或客户报告正文。
