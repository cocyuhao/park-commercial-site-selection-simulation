# 数据抽取目录

本目录保存从原始文件中抽取出的文本、表格、图表和日志。

子目录：

- `pdf_text/`：PDF 文本抽取结果。
- `ppt_text/`：PPT 文本抽取结果。
- `tables/`：结构化表格。
- `charts/`：图表截图或图表数据。
- `extraction_logs/`：抽取日志、错误、元数据。
- `scripts/`：抽取脚本。

抽取原则：

- 先抽取元数据和少量样本，再批量抽取。
- 抽取结果必须记录来源文件、页码或幻灯片号。
- 空白、全 0、异常单位、疑似截断的内容必须进入质量检查。
