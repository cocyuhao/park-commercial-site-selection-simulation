# 输出目录

本目录保存分析输出，不保存未经处理的原始文件。

子目录：

- `processed_tables/`：清洗后的模型输入表。
- `maps/`：点位、POI、缺口热力图。
- `figures/`：报告图表。
- `simulation_results/`：仿真结果。
- `recommendations/`：推荐业态和点位方案。

所有输出必须能回溯到 `40_quality_evidence/evidence_ledger.csv` 或模型配置。
